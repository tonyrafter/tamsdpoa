<!-- 
README file for data directory
-->

## Data directory README

This directory should contain:
1. the indices to analyse (expected in directories named `Indices_1min` and `Indices_5min`), 
along with
1. shapefiles (in directory named `shapefiles`) and
1. (optionally) global mean surface temperature data (in directory named `temperature`).


